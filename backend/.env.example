# ──────────────────────────────────────────────────────────────────────────────
# GoHighLevel (GHL) API credentials
# ──────────────────────────────────────────────────────────────────────────────
# 1. Log into your GHL account
# 2. Go to: Settings → Integrations → Private Integrations → Create new
# 3. Grant scopes: contacts.write, invoices.write, invoices.readonly
# 4. Copy the token below:
GHL_API_KEY=your_private_integration_token_here

# 5. Go to: Settings → Business Profile → scroll to "Location ID"
# 6. Copy the ID below:
GHL_LOCATION_ID=your_location_id_here

# ──────────────────────────────────────────────────────────────────────────────
# Email (contact & fleet forms) — supports both Resend and SMTP
# ──────────────────────────────────────────────────────────────────────────────
# Option 1: Resend (free, resend.com)
# 1. Sign up at resend.com (free — 3,000 emails/month)
# 2. Go to API Keys → Create API Key
# 3. Paste it below:
RESEND_API_KEY=re_your_api_key_here

# Option 2: SMTP (Zoho recommended — you own the domain)
# 1. Zoho Mail → My Account → Security → App Passwords → create one for "SMTP"
# 2. Paste the generated password (NOT your Zoho login password) into SMTP_PASS
# SMTP_USER must be the exact mailbox address you're authenticating as.
SMTP_HOST=smtp.zoho.com
SMTP_PORT=465
SMTP_USER=lucas@precisionworksdetailing.com
SMTP_PASS=your_zoho_app_password_here
CONTACT_EMAIL=lucas@precisionworksdetailing.com

# ──────────────────────────────────────────────────────────────────────────────
# Database (if used)
# ──────────────────────────────────────────────────────────────────────────────
# DATABASE_URL=postgresql://user:password@localhost:5432/mydb
